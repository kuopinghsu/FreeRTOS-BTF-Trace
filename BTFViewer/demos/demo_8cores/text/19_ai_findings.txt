Ask with the templates in a sensible order. Start with Analysis Findings — that prompt already names metrics and units. Your text appears in the You bubble; the model reply in Assistant. Read the severity, what it means on this multi-core system, and which Statistics section to open next. If the reply includes jump, colon, and a timestamp, that number uses the trace time unit — microseconds on this file. Click it: the timeline seeks and a note is dropped. Confirm what you see against the Statistics tables. Treat the assistant as a triage aid, not ground truth — especially on a demo file that stitches several tests together.
