Clear the cursors and return to the full trace. Step 6 checks migration and synchronization behavior.

The report records 18,992 core migrations. CS[20] has 595 migrations, a rate of about 1,644 per second, an average dwell near 501 microseconds, and repeated ping-pong movement. Similar patterns appear on several CS tasks.

Do not call migration a defect from the count alone. First confirm that load distribution is already balanced, then check migration rate, ping-pong, dwell time, core-pair movement, and synchronization bounce.

The report also finds synchronization objects that cross core boundaries while held. That is a separate issue from ordinary task migration and should be checked with the Mutex, Queue, and migration views.
