Clear the cursors so we are back on the full trace. Sixth rung: concurrency — migrations, heatmap, locks, inheritance. task CS eighteen migrates about fifteen hundred sixty times per second, with about half a millisecond average dwell on each core and a high ping-pong count — classic back-and-forth thrash, not lock contention. Thrash is usually an affinity or too-many-equal-workers problem. Bounce is an ownership problem: a lock held while the holder moves core. In Task view, lock-highlight the task; with Load on, the strip shows that task’s share per core. Toolbar Heatmap and Chord follow the visible viewport — zoom first. Use Lock Bounces Only when chasing mutex hops. Now open Mutex and Queue. One mutex has hundreds of holds and a handful of core bounces — status Warning. The queue is the headline: eight hundred fifty-eight of eight hundred sixty-four holds crossed a core while still held. Status can still read OK. Check every Warning row on the timeline before treating teardown noise as a real bug.
