Seventh rung: did the system do what the firmware asked? Open Core Affinity. Mask is what the task requested; Observed Cores is where it actually ran; Violations lists cores outside that mask. On this trace the Affinity tasks stay inside their masks — Violations stays empty. That is a pass. Open Task Lifecycle. Tasks SR zero through SR three show suspend and resume counts of four and four. Click SR zero and confirm it does not run between suspend and resume. Runs counts how many times the scheduler dispatched the task — much larger than four, and that is normal. Deadlines and tags are the rest of this rung: set thresholds in nanoseconds under Settings, Display, Analysis thresholds.
