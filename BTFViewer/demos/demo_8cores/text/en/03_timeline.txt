The main area is the timeline. Each colored bar represents one execution slice of a task. Move the pointer over a bar to see its duration, core, and nearby activity. The status bar shows the trace duration, active cores, and context-switch count.

This trace spans 2.521 seconds. Its timestamps start at about 1.014 seconds rather than zero, because BTFViewer keeps the timestamps from the original trace capture.
