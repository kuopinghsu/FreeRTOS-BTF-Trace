The main area is the timeline. Each colored bar is one execution slice of a task. Move the pointer over a bar to see its duration, its core, and the nearby slices. The status bar shows the full duration, the active cores, and the context-switch count. This trace runs 2.421 seconds, and its timestamps start near 1.014 seconds, not at zero.
