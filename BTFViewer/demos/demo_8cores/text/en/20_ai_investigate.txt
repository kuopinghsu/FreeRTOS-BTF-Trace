Step 10: investigate and verify a finding.

In this recorded example, the top actionable finding is suspected priority inversion. The AI investigation focuses on Low[266] and returns a Confirmed verdict.

The evidence chain identifies a priority-inheritance trigger at jump:3101862. Low[266] is boosted from base priority 2 to peak priority 4 until jump:3207230, a range of about 105.4 milliseconds. The investigation also finds an off-CPU gap of about 13.6 milliseconds beginning at jump:3101921.

Read the result from the conclusion outward: first the verdict, then the evidence chain, confidence and coverage, alternative explanations, and the recommended validation step. Detailed checks, supporting evidence, and investigation history can be expanded when needed.

The important point is that the AI conclusion is tied to measurable trace evidence and jump times that you can verify yourself.
