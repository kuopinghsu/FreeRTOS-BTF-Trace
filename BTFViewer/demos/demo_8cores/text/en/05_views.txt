Load draws CPU utilisation under the timeline. Core view stacks activity per core. Task view shows one row per task. Drag the divider if you need more room for the chart. Switch between Task and Core first, then turn Load on — only then are later statistics trustworthy.
