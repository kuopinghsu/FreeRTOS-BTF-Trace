Load displays CPU utilization below the timeline. Core View groups execution by core, while Task View gives each task its own row. Drag the divider when you need more room for the chart.

Choose the view that matches the question. Then use Load to compare CPU activity with the execution slices on the timeline.
