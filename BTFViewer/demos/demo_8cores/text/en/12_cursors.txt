Second rung: scope the phase that matters. Mixed traces hide issues inside setup, stress, and teardown. Place two cursors around the priority-inversion demo — the Low, Med, and High tasks near three point zero eight five to three point three one zero on the time axis. Enable Limit to C one through C n so Statistics and Analysis describe only that window. The status bar shows the span between the cursors. Re-open Analysis if you want findings for this window only. We clear the cursors next so the following core-balance numbers are full-trace. We will put this same window back for latency.
