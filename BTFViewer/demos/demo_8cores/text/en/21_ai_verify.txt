Step 11: verify the AI result against BTFViewer.

Click each jump:TIME link and inspect the corresponding event on the timeline. For this case, verify the priority-inheritance trigger, the boost from priority 2 to 4, the boost duration, and the nearby off-CPU interval. Then compare those events with Priority Inheritance, Mutex Blocking, and the related Statistics data.

The Evidence and Validation information records the finding, checks, alternative hypothesis, missing evidence, and supporting timeline matches. A Confirmed verdict is useful only when its cited evidence is inside the active scope and agrees with the measured data.

If the AI explanation and the trace disagree, use Statistics and the timeline as the source of truth.
