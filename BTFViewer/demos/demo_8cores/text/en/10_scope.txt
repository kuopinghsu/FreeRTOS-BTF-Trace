Step 2: narrow the analysis when the trace contains several test phases.

For the priority-inversion example, place two cursors around the relevant phase, roughly from 3.089 to 3.310 seconds, and enable Limit to C1 through Cn. Statistics and Analysis will then use that cursor range as the active scope.

Rebuild the findings for the selected range before comparing numbers or asking AI. We will return to the full trace for global checks, then use a focused cursor range again when verifying the priority-inversion evidence.
