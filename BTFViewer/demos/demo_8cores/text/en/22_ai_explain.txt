Step 12: keep a focused investigation inside its selected scope.

Place cursors around the phase you want to explain and enable Limit to C1 through Cn. Then run the scoped AI request. Every jump:TIME used as evidence should fall inside that cursor range.

If an evidence link is outside the active scope, do not use it to support the conclusion. Recheck the scope, rebuild the findings for that range, and run the investigation again.

After the cause is supported by measured evidence, you can use Experiment or What-if to explore a possible change. These are estimates, not measured validation. Make the firmware change, capture another trace, and use Compare to verify the result.
