Eighth rung: keep a record, and prove a fix. Export HTML and CSV follow the current cursor scope, and the HTML report puts Analysis Findings near the top. Trace Compare is on the toolbar after Analysis; it needs two traces open in separate tabs. Compare matching phases — same cursors on both sides — after a firmware change. A fix is not validated until a second trace proves it.
