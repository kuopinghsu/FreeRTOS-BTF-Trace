Next, explain one phase — not the whole stitched file. Place two cursors on the inversion window, three point zero eight five to three point three one zero, and enable Limit to C one through C n. Click Explain region. The viewer puts those bounds in the prompt, so every jump must land between the cursors. If a jump seeks elsewhere, reject that claim.

When you are done, you can export a diagnostic report from the AI tab, or open Compare after a firmware change. That is the whole newbie loop: evidence first, Start Investigation, click every jump, then Explain region inside C one through C n.
