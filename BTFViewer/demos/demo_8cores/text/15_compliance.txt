Last check: did the system do what the firmware asked? Open Core Affinity. Mask is what the task requested; Observed Cores is where it actually ran; Violations lists cores outside that mask. On this trace the Affinity tasks stay inside their masks — Violations stays empty. That is a pass. Open Task Lifecycle. Tasks SR zero through SR three show suspend and resume counts of four and four. Click SR zero and confirm it does not run between suspend and resume. Runs counts how many times the scheduler dispatched the task — much larger than four, and that is normal. After a firmware change, open a second trace in another tab and use Trace Compare, or Export HTML for a report you can share.
