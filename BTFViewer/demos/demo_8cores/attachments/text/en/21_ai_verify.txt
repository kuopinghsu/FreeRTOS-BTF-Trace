Step 11: verify the finding against BTFViewer.

Select the Off-CPU finding and use Verify finding. The assistant calls investigate on that finding id, re-collects evidence with metric and timeline queries, runs verify_claim and challenge_conclusion, then sets cursors and zooms to the strongest evidence.

The second pass returns Confirmed again, now scoped to the cursor window range:3087568/3090460. Its evidence chain: the 679 us blocking gap from jump:3088582 to jump:3089261 on Core_0, and the 335 us execution slice at jump:3087568, with C1 at jump:3087568 and C2 at jump:3090460. Confidence is High, quality Direct, coverage Complete for that window.

The Evidence and Validation panel scores it another way: evidence quality Medium-High at 70, coverage 95 percent, 19 of 20 claims directly observed, one assumption still unverified. A Confirmed verdict only counts when its cited evidence sits inside the active scope and matches the measured data.

Now click each jump:TIME in the reply and inspect the event on the timeline. Confirm the 679 us gap, the recurring 13 us gaps, and the execution slices, then cross-check them against the Off-CPU Time and Mutex Blocking Statistics pages.

As you confirm each jump, record it in the Investigation Notebook: a Verification bookmark with the finding and time range attached. Add to investigation on the finding, or Add region to investigation from the timeline right-click menu, fills the reference in for you. If the AI explanation and the trace disagree, Statistics and the timeline are the source of truth.
