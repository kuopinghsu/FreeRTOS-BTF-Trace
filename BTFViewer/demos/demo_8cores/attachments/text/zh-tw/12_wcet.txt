第四階段：把 CPU 使用量和過長的單次執行分開看。

「熱門任務」顯示多個 CS 工作任務的 CPU 占比接近，都約占有效 CPU 時間的 14%。CPU 占比高，不代表同一個任務也有最長的單次執行。

「每次執行時間」顯示，使用者任務中最長的觀察值來自 NC[118]，約 110.2 毫秒。這是實際觀察到的最大值，不是已證明的 WCET。報告也會標出 Max 明顯高於平常執行時間的任務，例如 Runner[1] 與數個 CS 任務。

點選 Max 回到時間軸找到那段執行，再配合搶佔與同步資料判斷原因，不要只靠單一數值下結論。
