從活動列開啟「分析」。

這裡會整理目前範圍內最值得調查的線索。這份追蹤包含核心負載均衡、CPU 使用量較高的任務、過長的執行區間、離開 CPU 的延遲、疑似優先權反轉、頻繁核心遷移、tick 健康警告、同步物件跨核心彈跳，以及執行時間尖峰。

每項發現都會附上量測證據，並指出下一個要查看的統計區段。信心程度也很重要：直接量測的核心使用率與執行時間可信度較高；原因可能混合，或依門檻判定的項目，則需要更多驗證。

把「發現」當成找方向的入口，再用「統計」與時間軸確認證據，不要直接把它當成結論。
